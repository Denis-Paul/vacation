<%# $("#city-weather-data").html("<%= j render 'weather_data', city_weather: @city_weather >"); %>

// render weather data
var body = document.getElementById("city-weather-data");
body.innerHTML = "<%= j render 'weather_data' %>" + body.innerHTML;

// clear and focus on input
var input = document.getElementById("search-city");
input.value = '';
input.focus();